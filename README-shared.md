# Shared Folder Migration

All files from `shared/` have been copied and synchronized to their respective folders:

- `components/common/`
- `constants/`
- `hooks/`
- `services/`
- `styles/`
- `types/`
- `utils/`

You can now safely remove the `shared/` folder and its contents.
